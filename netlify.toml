[build]
  publish = "."
  command = "echo 'Static site, no build command needed'"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
